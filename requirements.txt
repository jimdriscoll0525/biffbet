# Core data / numerics
pandas>=2.0
numpy>=1.24
pyarrow>=12.0          # parquet engine for the cache layer

# HTTP + resilience
requests>=2.31
tenacity>=8.2          # retry/backoff for every external API call

# Baseball data
pybaseball>=2.2        # Statcast + FanGraphs pulls

# Football data (football/ subpackage only)
nflreadpy>=0.1         # nflverse pbp/EPA, schedules, snap counts, FTN (pulls polars)

# Config / env / CLI / output
python-dotenv>=1.0
PyYAML>=6.0
click>=8.1
rich>=13.0             # pretty terminal tables + logging
streamlit>=1.30        # local web viewer (python -m mlb_value_bot serve)

# NOTE on Python version:
#   Targets Python 3.11+ on a 64-bit interpreter. Verified on 64-bit Python
#   3.13.7 (pybaseball 2.2.7, pyarrow 24, pandas 3.0, scipy). Use 64-bit: pyarrow
#   ships no 32-bit Windows wheel. 3.14 may lack some scientific wheels; if pip
#   struggles, use 3.13. The bot degrades gracefully if pybaseball is missing or
#   FanGraphs is unreachable (lower data-completeness / confidence; see README §7).
